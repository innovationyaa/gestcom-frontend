import React, { useState } from "react";
import { Package, Save, Loader2, ChevronDown } from "lucide-react";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
} from "@/components/ui/dialog";

// Deprecated global StockEditModal
// The stock edit modal has been moved to the stock feature at:
// src/features/stock/components/StockEditModal.jsx
// Keep this file as a harmless stub to avoid accidental imports during refactors.

export default null;
